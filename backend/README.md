# Backend

Django backend foundation for `hesabdari_balkh`.

The current development database is SQLite for offline, single-user development. Do not add business workflows until the relevant Phase 0 contract is approved.
